// AI/CHAIN/CHAIN.CPP -- (NOT USED YET!!!)
# include "chain.h"

    // Need to use CHAIN.H to build chain dynamics in architecture.

Chain & chainCreate() {
    Chain *c = new Chain;
    return *c;
}

void readChain(Chain c) {
    // enumerate chain
}

void writeChain(int x) {
    // write to chain
}

void writeChainData(char * str) {
    
}